# Report: History-enabled vs standard scrollbar (participant p1)

**Data file:** `history_scrollbar_study_1774668356099.json`  
**Participant:** p1  
**Order:** BA (Condition B first, then Condition A)  
**Study design:** `return_navigation_ab` — two visit tasks (anchors saved), then two return tasks scored against those anchors.

---

## 1. Summary: why this participant favors the history-enabled scrollbar

The log supports a preference for the **history-enabled** scrollbar through three converging lines of evidence: **(a) return accuracy**, **(b) amount of scrolling on return tasks**, and **(c) Likert ratings**, especially **ease** for jumping back.

---

## 2. Behavioral evidence: return tasks

### 2.1 Accuracy (return only)

| Return task | Condition B | Condition A |
|-------------|-------------|---------------|
| Return to Task 1 anchor | **Correct** (final ratio very close to saved position ~0.174) | **Incorrect** (target ~0.155, landed ~0.060 — far from saved spot) |
| Return to Task 2 anchor | **Correct** (matches saved ratio in log) | **Correct** |

Under **B**, both returns succeeded. Under **A**, half of the returns failed on first submission; the miss on “return to Task 1” is large, not a borderline tolerance issue.

This is **objective evidence** in this file that, for this participant and session, the **standard scrollbar performed worse on return accuracy** than the history-enabled condition.

### 2.2 Effort proxy: scroll event counts on return tasks

- **B:** 1 scroll event on return task 3, 1 on return task 4 → **2 total**  
- **A:** 135 on return task 3, 157 on return task 4 → **292 total**

Under B the participant barely scrolled during returns (consistent with using **marker jumps** or a single discrete action). Under A they scrolled heavily while re-finding prior positions. This supports a **lower-trial** strategy on returns when history markers are available.

### 2.3 Time on returns (interpret with accuracy)

- **B** (returns 3 + 4): approximately **40.4 seconds** total  
- **A** (returns 3 + 4): approximately **17.8 seconds** total  

A was faster on the clock, but **one return was wrong** — faster completion does not mean better performance. B took longer but **met the return criterion both times** with **minimal scrolling**.

---

## 3. Subjective evidence: Likert (after each block)

**After Condition B (history-enabled):**

- **ease:** 5 — jumping back felt very easy  
- **necessity:** 4 — strong agreement that this scrollbar helped avoid excessive trial-and-error on returns  
- **wantFeature:** 5 — strong desire for scroll history on the scrollbar  

**After Condition A (standard):**

- **ease:** 1 — jumping back felt very difficult  
- **necessity:** 5  
- **wantFeature:** 5  

The clearest contrast is **ease (5 vs 1)**, aligned with **failed return** and **heavy scrolling** under A. The high **necessity** and **wantFeature** scores after A may reflect wording or order effects; the primary argument rests on **ease** plus **objective return metrics**.

---

## 4. Order effects (BA) and limits of generalization

The participant experienced **B before A**, so practice on the four-task structure could help A. Even so, under A they still **missed** the first return and reported **ease 1**, which supports tying the difference to **scrollbar affordance**, not only learning.

**Limitation:** This is **one participant**. Pooling more sessions and accounting for **AB vs BA** order strengthens claims beyond this single file.

---

## 5. Conclusion (one sentence)

For participant p1, the history-enabled scrollbar is supported as **better for return navigation** because return tasks were **100% accurate** with **almost no scrolling** under B, while under A the **first return failed badly** despite **heavy scrolling**, and **Likert ease** for jumping back was **maximal for B** and **minimal for A**.

---

*Generated for course documentation from exported study JSON.*
